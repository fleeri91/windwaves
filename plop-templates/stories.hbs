import React from 'react';
import { Meta, Story } from "@storybook/react";
import {{properCase name}} from './{{properCase name}}';

type {{properCase name}}Props = React.ComponentProps<typeof {{properCase name}}>;

/* eslint import/no-anonymous-default-export: [2, {"allowObject": true}] */
export default {
	title: 'WindWaves/{{properCase type}}/{{properCase name}}',
	component: {{properCase name}},
} as Meta<{{properCase name}}Props>;

const Template: Story<{{properCase name}}Props & {}> = (args) => (
  <{{properCase name}} {...args} />
);

export const Default = Template.bind({});

Default.args = {};

Default.parameters = {
	controls: {
		include: Object.keys(Default.args),
	},
}